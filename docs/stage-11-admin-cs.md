# Tahap 11 — Admin dan CS

Admin memakai akun internal `vp_admin_users`, bukan Google customer. API awal mendukung login, daftar pesanan, pembaruan status, serta penyimpanan resi/delivery. Buat admin pertama secara aman pada database dengan `password_hash()` sebelum memakai UI.
